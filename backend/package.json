{
  "name": "leadflow-pro-backend",
  "private": true,
  "type": "module",
    "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npx prisma db push --schema=backend/prisma/schema.prisma && node backend/server.js",
    "postinstall": "npx prisma generate --schema=backend/prisma/schema.prisma"
  }
}
